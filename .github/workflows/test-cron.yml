name: Test cron

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:  # Run manually from Actions tab
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cron fired at $(date -u)"