IMAGE PROMPT CONTEXT FILES

These files are used in AI image generation prompts.
Keep each file under 333 characters to stay within the 1024 character prompt limit.

The filename (without .txt) becomes the trait value used in the prompt.
Example: "impressionist.txt" → "impressionist style"
