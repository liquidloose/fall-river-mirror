The full meeting transcript is available to you as cached content. The first-pass anchors are included below — use them as your coverage map. Per your gap-audit system instructions: first run the **mandatory opening check** (meeting date and attendance roll if missing), then extract **only** additional factual anchors hiding in time gaps the first pass skipped.

### MEETING METADATA
- YOUTUBE_VIDEO_ID: "{youtube_video_id}"
- MEETING_DATE: "{meeting_date}"

### FIRST-PASS ANCHORS (coverage map — additions only, do not re-emit)
{draft_anchors_json}

Return the structured JSON envelope conforming to the configured response schema. Do not include any text outside the JSON.
